{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <h1>Register</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword) }}
        {{ form_row(registrationForm.naam) }}
        {{ form_row(registrationForm.adres) }}
        {{ form_row(registrationForm.woonplaats) }}
        {{ form_row(registrationForm.telefoonnumer) }}
        {{ form_row(registrationForm.geboortedatum) }}
        {{ form_row(registrationForm.geslacht) }}


        <button type="submit" class="btn">Register</button>
    {{ form_end(registrationForm) }}
{% endblock %}
